package programmer.zaman.now.data;

public interface HasBrand {
    /**
     * You can inherit the interface to another interface using keyword extends on the interface.
     * You can also inherit multiple interface on one class (Example on Avanza.java)
     * On Java one interface can have more than one parent unlike normal class.
     *
     * @return brand
     */
    String getBrand();
}
